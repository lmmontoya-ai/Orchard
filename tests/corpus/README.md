# Test Corpus Notes

The files under `tests/corpus/samples` are M0 placeholders. They are intentionally tiny and exist to validate the build, fixture-manifest flow, and `orchard-inspect` integration path before real APFS fixtures are introduced.

Real APFS corpus images should be added later under a documented storage policy so large binary files do not enter the repository casually.

